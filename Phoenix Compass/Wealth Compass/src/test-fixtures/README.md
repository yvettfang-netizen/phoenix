# TEST_ONLY fixtures

This directory is reserved for infrastructure test fixtures. Every fixture must carry a `TEST_ONLY` marker. Fixtures must never be imported by production scoring, persona, or report modules and must never represent themselves as official Wealth Compass rules.
