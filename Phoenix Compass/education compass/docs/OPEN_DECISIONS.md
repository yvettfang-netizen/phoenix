# Open Decisions：¥39.90 Education Compass 与Agent V0.4.0上线前决策

下列事项不能由代码或演示数据替代。未关闭的高优先级事项会让生产购买保持 fail closed，但不阻止本地 Mock 和自动化测试。

当前工作副本未附带P0批准/配置证据：OD-01至OD-06不得视为关闭，`PAID_COMPASS_ENABLED=false`；OD-05、OD-06、OD-12和OD-15不得视为关闭，`OPENAI_AGENT_ENABLED=false`、`AI_WORKER_ENABLED=false`。`.env.example`、测试Key、Mock Provider、`store:false`、占位域名和自动化通过均不能替代真实审批。

| ID | 优先级 | 决策 | 负责人建议 | 未决时默认行为 |
| --- | --- | --- | --- | --- |
| OD-01 | P0 | 认证小程序 AppID、主体、服务类目及支付权限 | 产品/运营 | 保留 touristappid，仅可演示，隐藏真实购买 |
| OD-02 | P0 | 微信支付商户号与 AppID 绑定、API v3配置、退款权限 | 财务/支付管理员 | WeChat provider 拒绝启动 |
| OD-03 | P0 | 生产 API 域名、公网 HTTPS notify URL、request/download 合法域名 | 运维 | 真实下单禁用 |
| OD-04 | P0 | `verified` 院校/专业/资格 Source Catalog manifest、适用年份和核验人 | 教研/顾问 | placeholder/演示数据不得收费，报告QA不通过 |
| OD-05 | P0 | 隐私政策、用户协议、AI报告声明、监护人同意文本及版本 | 法务/产品 | 未获得有效同意不能提交或支付 |
| OD-06 | P0 | 生产 PostgreSQL、加密、备份、恢复、删除与审计策略 | 技术/安全 | 不保存生产交易 |
| OD-07 | P1 | 数字报告退款政策、退款后撤权例外/恢复及部分退款规则 | 法务/客服/财务 | 只开放管理员技术接口；退款成功安全默认撤权，不开放用户自助退款 |
| OD-08 | P1 | 报告访问期限、PDF下载期限和账号注销后的处理 | 产品/法务 | 权益无自动到期；上线前必须确认 |
| OD-09 | P1 | 学生资料修改后是否免费重算、重新购买或人工复核 | 产品 | 新测评形成新快照和新订单，不覆盖已购报告 |
| OD-10 | P1 | 发票、税务、账单商品名称和客服SLA | 财务/客服 | 页面只使用真实商品名，不承诺未配置的发票能力 |
| OD-11 | P1 | 交易类小程序是否需要订单发货管理及相应履约回传 | 运营/合规 | 真机生产验收前核对官方最新要求 |
| OD-12 | P0 | OpenAI独立Project/Key、批准模型、费用/速率告警、数据处理/保留/地域及低于适用数字同意年龄数据所需ZDR | 技术/法务/安全 | Agent和worker保持关闭，只运行Mock；`store:false`不构成批准 |
| OD-13 | P2 | 39.9报告与199会员未来的折扣、赠送或升级关系 | 商业负责人 | 两个SKU和权益完全隔离，无互赠、无折扣 |
| OD-14 | P2 | 运营数据是否镜像飞书及字段范围 | 运营/安全 | 不镜像支付密钥、问卷全文、OpenID或完整报告 |
| OD-15 | P0 | Agent未成年人披露、报告/会话专属监护人同意、内容分级、高风险/危机固定文案、人工升级和申诉SOP | 法务/产品/安全 | Agent和worker保持关闭；不向OpenAI发送学生关联内容 |
| OD-16 | P1 | Agent正文30天保留、删除SLA、备份到期、法定访问/删除/申诉与退款后正文访问政策 | 法务/隐私/运维 | 产品API退款后拒绝正文但保留删除/法定请求渠道；不宣称即时物理擦除 |

## 上线闸门

只有同时满足以下条件，才能把真实购买开关设为启用：

1. OD-01 至 OD-06 已关闭并留有配置/审批证据；
2. Source Catalog 与报告模板已通过教研抽检；
3. 自动化测试、数据库迁移和安全扫描全部通过；
4. 微信开发者工具预览及 iOS/Android 真机检查通过；
5. 正常支付、取消、回调延迟、主动查单、重复通知和退款完成受控测试；
6. 运维人员能够执行查单、对账、告警和密钥轮换手册；
7. 产品界面没有把 Mock、演示事实或前端回调显示为真实支付成功。

满足上述条件并形成发布审批记录后，授权发布人才能把 `PAID_COMPASS_ENABLED` 设为 `true`。紧急回滚先恢复为 `false`；该操作只阻止新的创单/预支付，不能取消已发支付参数或拒绝可信成功的在途交易。若回滚原因是Source Catalog撤回，受影响交易必须进入人工事故、退款、通知/查退款补偿和审计流程。

OpenAI Agent使用独立闸门。只有OD-05、OD-06、OD-12、OD-15关闭，并且Agent自动化、预发布真实API、未成年人/PII、删除和故障隔离清单留证后，授权发布人才能依次部署独立worker并开启`OPENAI_AGENT_ENABLED/AI_WORKER_ENABLED`。Agent紧急回滚只关闭Agent与worker，不关闭支付、飞书或已购报告/PDF；不自动执行生产down migration。
