# Phoenix Family OS V0.4.1 验证记录

> 本文件记录2026-08-22的离线代码级验证。它不代表真实OpenAI、微信支付、飞书或生产PostgreSQL已连接。

## 1. 候选信息

- 验证日期：2026-08-22
- 测试负责人：Codex（本地离线验证）
- Git commit / 包摘要：工作副本无`.git`；交付ZIP的SHA-256在交付说明中单独记录
- Node / npm 版本：Node `v24.18.0` / npm `11.16.0`
- 小程序候选版本：V0.4.1
- 服务端候选版本：V0.4.1

## 2. 自动化与构建

| 检查 | 命令 | 状态 | 证据/备注 |
|---|---|---|---|
| 服务端依赖锁定安装 | `npm.cmd --prefix server ci` | `NOT_RUN_THIS_PASS` | 依赖已存在；本轮未重复联网安装，不冒充新证据 |
| 全量测试 | `npm.cmd run test:all` | `PASS` | 客户端全部检查通过；服务端65/65通过、0失败 |
| 服务端 typecheck | `npm.cmd --prefix server run typecheck` | `PASS` | 由`test:all`中的`typecheck:server`执行 |
| 服务端 build | `npm.cmd --prefix server run build` | `PASS` | 由服务端测试入口先执行TypeScript build |
| 001→002→003→004 migration | `npm.cmd --prefix server run db:migrate` | `WAITING_FOR_CONFIGURATION` | 需要获批非生产 PostgreSQL |
| release 构建 | `npm.cmd run build:release` | `PASS_PLACEHOLDER_ONLY` | `.invalid` API与占位AppID；16源码页、14发布页、88文件，不可部署 |

## 3. V0.4.1 专项合同

- [x] 免费 `ASSESSMENT_ANALYSIS` 不要求付费权益，但要求owner、合格`PREVIEW_READY`测评、完整度≥70、有效同意与QA。
- [x] ¥39.90 `REPORT_ANALYSIS` 和 `REPORT_FOLLOWUP` 在创建、worker领取/提交及读取时均要求 `ACTIVE entitlement + READY + DELIVERED + qaPassed`。
- [x] 一次性分析POST、`GET /v1/agent-analyses/:runId`和两个资源`/latest`均通过；裸集合GET不作为API。
- [x] 三种taskType的报告/测评上下文仅含12个受控字段；自由文本答案、客户资料和六模块原文为零。
- [x] `REPORT_FOLLOWUP`只额外发送经本地PII/危机/注入/越权检查的追问文本及有限轮上下文。
- [x] API仅加密入队，独立worker异步调用Provider；Agent故障不影响支付、退款、报告/PDF或飞书。
- [x] `004_dual_agent_analysis.sql` 扩展三种purpose及 `(user_id, report_id, purpose)` 活动唯一索引；001—003校验和未改变，未来结构迁移从005起。
- [x] `FEISHU_CUSTOMER_PROFILE_FIELDS_ENABLED`默认false；开启时仅家庭5项、学生8项精确白名单，名单外字段被拒绝，客户资料不进入OpenAI。

## 4. 外部系统（不得预填通过）

| 系统 | 当前状态 | 通过所需证据 |
|---|---|---|
| OpenAI | `WAITING_FOR_CONFIGURATION` | 获批Project/Key/模型、数据处理与未成年人审批、费用告警、12字段出站抽样、删除/回滚SOP |
| 微信支付 | `WAITING_FOR_CONFIGURATION` | 认证AppID/商户、真实签名配置、公网HTTPS、受控真机扣款/退款/对账证据 |
| 飞书 | `WAITING_FOR_CONFIGURATION` | 获批App/Base/7表、最小权限、Schema校验、小批量reconcile；客户资料扩展另需隐私审批与删除演练 |
| PostgreSQL | `WAITING_FOR_CONFIGURATION` | 非生产/生产各自的迁移、TLS、备份恢复与访问控制证据 |

## 5. 最终结论

- 代码级结果：`PASS`（全量自动化、typecheck、build及占位release边界验证）
- 人工真机结果：`NOT_RUN`
- 外部阻塞项：真实凭据、获批非生产PostgreSQL、备案HTTPS域名、微信真机及第三方沙箱/生产联调
- 是否允许开启 `PAID_COMPASS_ENABLED`：否（待正式批准）
- 是否允许开启 `OPENAI_AGENT_ENABLED/AI_WORKER_ENABLED`：否（待正式批准）
- 是否允许开启 `FEISHU_CUSTOMER_PROFILE_FIELDS_ENABLED`：否（待正式批准）
