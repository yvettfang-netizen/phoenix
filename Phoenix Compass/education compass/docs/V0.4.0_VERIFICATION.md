# Phoenix Family OS V0.4.0 验证记录

验证日期：2026-08-22  
验证范围：本地代码、Mock/合同测试、静态迁移合同、默认安全启动、离线小程序 release。  
明确未授权：真实支付、生产迁移、OpenAI 实际请求、飞书生产写入、小程序上传或发布。

## 结果

| 类别 | 状态 | 证据 |
| --- | --- | --- |
| 自动化 | PASS | `npm.cmd run test:all`；客户端校验全部通过，服务端53/53通过 |
| 干净依赖 | PASS_WITH_RETRY | 全局 npm 缓存被 Windows 拒绝；项目内缓存重试成功，19包、0漏洞 |
| TypeScript/构建 | PASS | server typecheck、build、test分别复跑通过 |
| Mock 闭环 | PASS | 付费报告门禁、独立同意、加密入队、worker、来源映射、安全阻断、删除/退款/配额 |
| 默认本机启动 | PASS | 外部集成全关时 `/health` 返回 `{"ok":true}` |
| 离线 release | PASS | `.invalid` 构建成功；15个源码页面、13个release页面、83个文件、敏感边界扫描0命中 |
| PostgreSQL 迁移执行 | WAITING_FOR_CONFIGURATION | 本机无 psql、Docker 或 PostgreSQL 服务；仅完成静态合同与哈希验证 |
| OpenAI 实际调用 | WAITING_FOR_CONFIGURATION | 未提供或使用批准的 Project、Key、模型与合规证据 |
| 微信支付/退款沙箱或真机 | WAITING_FOR_CONFIGURATION | 未进行真实交易；原有 Mock/合同回归通过 |
| 飞书真实多维表格 | WAITING_FOR_CONFIGURATION | 未使用真实凭据；原有 client/schema/sync 回归通过 |
| 小程序上传/发布 | NOT_STARTED | 本次只生成离线测试包，未上传或发布 |

## 迁移

- `001_initial_schema.sql`：`502AB6BED513978922FAD8FD424D1C11281B07771E37CE056CF001A2674B35C9`
- `002_feishu_bitable_integration.sql`：`5A9FC3092BDF46025834A1211E8458CBFF9D3B1DD39ECBF3C2BD02A72CA2D34D`
- `003_openai_agent.sql`：`1E8B64E1FE38D48BEFD11AD3D69AF73423D77AF17A294ECF8E809A83C3D197E6`

001与002未改。003新增独立Agent同意、会话、加密消息、异步run和worker heartbeat表；无明文正文列或OpenAI response ID列。正式迁移只能在获授权且完成备份的本地/测试/预发布PostgreSQL执行。

## 回滚与残余风险

- 回滚顺序：关闭 `OPENAI_AGENT_ENABLED` 与 `AI_WORKER_ENABLED`，停止独立worker，保留003表和加密内容；不自动执行生产down migration。
- PostgreSQL专用 claim 使用短事务与 `FOR UPDATE SKIP LOCKED`；通用事务对 `40001/40P01` 最多重试三次，但专用 claim 尚无同级死锁重试，需在真实并发压测中观察并告警。
- 默认速率限制器按API进程内存计数；多实例生产需由网关或共享限流补充。
- 确定性PII规则不能替代DLP、人工升级和未成年人合规流程；`store:false` 也不能替代ZDR或数据处理批准。
- OpenAI上游在模糊超时/进程崩溃时可能至少一次执行；只保证Phoenix本地run幂等，不保证上游exactly-once。
